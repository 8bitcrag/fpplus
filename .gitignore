/bin-data
raw-data
wireframes
dev
raw

# tools/ is scratch, except the build and the test runner
tools/

# the vendored minifier -- docs/ is the build output and IS committed,
# because GitHub Pages serves the site from it
/packages

CLAUDE.md
AGENTS.md
launch.json
.claude
js/search/binary/README.md
.vscode
# until ready
tests
/css
/js
/index.html
